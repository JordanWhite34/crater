# Synthetic Humvee matched-style expansion v2

Eight additional documentary-style synthetic images generated using the real
Humvee photos as visual references. They are training-only candidates and must
remain excluded from validation and test splits.

The intended canonical labels are recorded in `labels.csv`. Coordinates are not
asserted because they require human CVAT review.
